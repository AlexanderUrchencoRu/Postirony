meta:
  environment: python
  toolchain:
    name: pip
    version: latest
build:
  requirementsPath: requirementsPath
run:
  scriptName: scriptName
  command: gunicorn --bind 127.0.0.1:5000
  persistenceMount: persistenceMount
  containerPort: 5000
